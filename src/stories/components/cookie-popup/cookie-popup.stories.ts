import { createCookiePopup } from './cookie-popup';

const meta = {
  title: 'components/cookie-popup',
  tags: ['autodocs'],
  render: createCookiePopup,
};

export default meta;

export const CookiePopup = {};
