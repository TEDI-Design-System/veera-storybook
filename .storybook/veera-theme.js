import { create } from '@storybook/theming/create';
import logo from '../public/logo_eu_regionaldev.svg';

export default create({
  base: 'light',
  brandImage: logo,
  brandTitle: 'Veera Storybook'
});
